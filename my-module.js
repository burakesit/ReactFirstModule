 const hello = (name) => {
    console.log("Hello " + name);
    console.log(`hello ${name}`)
};

 const topla = (a, b) => a + b;
 const cikar = (a, b) => a - b;

 const text = 'Text';
 const user ={
    name:'Mehmet',
    surname:'Seven'
 }
 const users = [{
name:'Ali',
surname:'Veli'
 },
{name:'Kemal',
surname:'Sunal'}]


export {
    hello,
    topla,
    cikar,
    user,
    users
};


export default text;

